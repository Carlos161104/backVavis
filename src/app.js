// Initialize the Express configuration
import express from "express";
const app = express();

// Routes imports

// Middlewares
app.use(express.json());

// Use of routes

// Export the app
export default app;
